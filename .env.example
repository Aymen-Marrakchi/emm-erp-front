# ── Frontend environment (copy to .env.local and fill in) ───────────────────

# Base URL of the backend API — must include the /api suffix.
# Development:
NEXT_PUBLIC_API_URL=http://localhost:5000/api
# Production example:
# NEXT_PUBLIC_API_URL=https://api.mycompany.tn/api
